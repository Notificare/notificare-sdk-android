<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Sample" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        -></style>

    <style name="Theme.Sample" parent="Base.Theme.Sample">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.App.Notificare" parent="Base.Theme.Sample">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.App.Notificare.Translucent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.App.Notificare.Iam">
        <!-- Disable the ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Apply a translucent background effect -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@color/notificare_translucent</item>

        <!-- Customise your theme below -->
        <item name="cardViewStyle">@style/Widget.App.CardView.Filled</item>
    </style>

    <style name="Widget.App.CardView.Filled" parent="Base.Widget.Material3.CardView">
        <item name="cardCornerRadius">8dp</item>
    </style>

</resources>
