<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="re.notifica.geo.hms">

    <application>

        <receiver
            android:name="re.notifica.geo.hms.LocationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="re.notifica.intent.action.LocationUpdated" />
                <action android:name="re.notifica.intent.action.GeofenceTransition" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
